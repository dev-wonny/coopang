rootProject.name = 'coopang'

// CLOUD (System)
include 'CLOUD:eureka'

// DATA (Domain)
include 'DATA:api-data'

// INFRA (Integration)
include 'INFRA:api-config'
include 'INFRA:api-communication'

// BOOT (Server)
include 'BOOT:gateway', 'BOOT:user', 'BOOT:hub', 'BOOT:delivery'