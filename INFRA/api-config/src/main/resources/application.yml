spring:
    application:
        name: api-config
    autoconfigure:
        exclude:
            - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

common:
    api:
        filter:
            enabled: true
            paths:
                - /swagger-ui
                - /v3/api-docs
                - /actuator